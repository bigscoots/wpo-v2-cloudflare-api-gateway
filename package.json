{
	"name": "bigscoots-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "npx wrangler deploy --dry-run",
		"deploy": "npx wrangler deploy",
		"dev": "npx wrangler dev",
		"start": "npx wrangler dev",
		"test": "vitest",
		"test:hmac": "node test/hmac-gateway-test.js",
		"cf-typegen": "npx wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.38.0"
	}
}